import UserDashboard  from './components/UserDashboard';
const routes = [
	{ path:'/', component:UserDashboard, meta:{ pageTitle:'User Dashboard', bredCrumbs:['Home','Dashboard']}}

];

export default routes;
